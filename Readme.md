How to run tests:

1. Create python venv: python3 -m venv .venv
2. Create python venv: source .venv/bin/activate
3. Install dependencies: pip install -r requirements.txt 
4. Run tests: pytest (to call only api tests: pytest api, to call only ui tests: pytest ui)